# Per UGOS Pro / Docker → Progetto.
# Non serve build: scarica l'immagine già pronta.

services:
  web:
    image: ghcr.io/sarlokko/cercatrova:latest
    container_name: il-cerca-trova
    ports:
      - "8787:80"
    restart: unless-stopped
